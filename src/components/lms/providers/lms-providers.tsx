"use client";

import type { ReactNode } from "react";

/**
 * Learn route providers — Motion, event bus, a11y (Phase B).
 * @see docs/ljp-spec/implementation-roadmap.md
 */
export function LmsProviders({ children }: { children: ReactNode }) {
  return children;
}

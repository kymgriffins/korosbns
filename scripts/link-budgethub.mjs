// /budgethub has been removed (redirected to / in next.config.ts)
// This script is kept as a no-op to avoid breaking any existing CI pipelines.
console.log("budgethub is disabled — all /budgethub/* routes redirected to /");

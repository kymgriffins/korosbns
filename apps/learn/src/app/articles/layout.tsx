export const metadata = {
  title: "Budget Articles & Explainers | Budget Ndio Story",
};

export default function LearnArticlesLayout({ children }: { children: React.ReactNode }) {
  return children;
}

# Review: LJP-002 — learning-runtime

```yaml
item: LJP-002
capability: learning-runtime
date: 2026-07-07
status: PASS
evidence_required: true
```

---

## Summary

Event-sourced **Learning Runtime** implemented at `src/lib/learning-runtime/`. Session, progress, timeline, navigation derived from append-only events. Provider wired in `learn/layout.tsx` (composition with shell). Runtime DoD: deterministic, replayable, observable, recoverable — all tested.

---

## Requirements

| REQ | Evidence |
|-----|----------|
| REQ-0100 | `reducer.ts` — progress only via `reduceEvents`; no UI mutation |
| REQ-0101 | `runtime.ts` dispatch append-only; state from fold |
| REQ-0102 | `timeline.ts` + `getTimeline()` |
| REQ-0103–0105 | Event types in `types.ts`; bridge in `events.ts` |

---

## Sub-capabilities

| Sub | PASS evidence |
|-----|---------------|
| event_store | `runtime.ts` dispatch/replay |
| session | `SessionStarted` in reducer |
| progress | `LessonCompleted`, `VideoPaused`, etc. |
| timeline | `eventsToTimeline` |
| navigation | `navigation.ts` resolveNavigation |
| persistence | `persistence.ts` sessionStorage |
| lesson_phase | `deriveLessonPhase` in reducer |
| runtime_provider | `context.tsx` + layout wiring |

---

## Runtime DoD

| Gate | Evidence |
|------|----------|
| Deterministic | `runtime.test.ts` same events → same state |
| Replayable | `replay` test |
| Observable | timeline 1:1 test |
| Recoverable | `hydrate` test |

---

## Traceability

```
INTENT-002 → CAP-learning-runtime → CMP-LearningRuntime → learning-runtime@1.0.0
```

Contracts: `agent/spec/runtime-contracts/*.md`

---

## Composition note

Shell unchanged. Runtime Provider wraps shell in layout — first composition layer for LJP-003 validation path.
